import styled from "styled-components";

export const StyleInput = styled.div`
  width: 343px;
  height: 52px;
  padding: 10px;
  font-size: 1rem;
  font-family: "muli";
  color: var(--text);
  border-radius: 5px;

  ::-webkit-input-placeholder {
    color: var(--text);
    font-family: "muli";
  }
`;
